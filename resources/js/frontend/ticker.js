export function initializeTickers(){document.querySelectorAll('[data-ticker]').forEach(ticker=>{const toggle=ticker.querySelector('[data-ticker-toggle]'),label=ticker.querySelector('[data-ticker-control-label]');if(!toggle)return;let paused=matchMedia('(prefers-reduced-motion: reduce)').matches;const render=()=>{ticker.classList.toggle('is-paused',paused);toggle.setAttribute('aria-pressed',String(paused));label.textContent=paused?'Resume breaking news':'Pause breaking news'};toggle.addEventListener('click',()=>{paused=!paused;render()});document.addEventListener('visibilitychange',()=>ticker.classList.toggle('is-page-hidden',document.hidden));render()})}
