# Existing state audit

Media stores disk/path, MIME, dimensions, checksum, metadata and featured post references. WordPress paths are normalized by `MediaPathNormalizer`; public URLs resolve through the configured disk and storage symlink. Originals are never overwritten.
