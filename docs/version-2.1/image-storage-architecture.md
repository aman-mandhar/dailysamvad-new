# Storage

Derivatives use `media/derivatives/v1/{media-id}/{width}.{format}`. Source disk/path and URL remain unchanged. Metadata JSON records verified path, dimensions, format, byte size and timestamp.
