# Presets

Central presets are `thumb` 320×180, `card` 640×360, `article` 1280×720 and `hero` 1600×900. Responsive widths are 320, 640, 960, 1280 and 1600; widths larger than the source are never generated.
