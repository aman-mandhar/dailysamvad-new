# Baseline

Capture representative anonymous pages with transfer size, image bytes, LCP and CLS before activation. This repository has no browser runner, so production/browser measurements remain an operational prerequisite.
