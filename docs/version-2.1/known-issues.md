# Known issues

Local Redis, supervised workers, production proxy configuration, browser performance tooling, backup restore verification and production smoke testing are unavailable in this environment.
