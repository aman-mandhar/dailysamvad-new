# Responsive component

`x-news.responsive-image` supports `srcset`, `sizes`, intrinsic width/height, optional `<picture>` sources, alt text, and an explicit fallback. Existing article/card templates remain compatible.
